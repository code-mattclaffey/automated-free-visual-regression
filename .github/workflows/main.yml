name: "CI/CD Main"

permissions:
  contents: "read"
  id-token: "write"

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  storybook-visual-chromium:
    uses: ./.github/workflows/playwright.yml
